apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: assetflow-trade-mongo-pvc
  labels:
    app: assetflow-trade-mongo
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
