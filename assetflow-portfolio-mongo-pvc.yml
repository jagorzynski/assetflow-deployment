apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: assetflow-portfolio-mongo-pvc
  labels:
    app: assetflow-portfolio-mongo
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
